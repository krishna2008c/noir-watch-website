# NOIR — Luxury Watch Concept

Static HTML/CSS/JavaScript portfolio project.

## GitHub Pages
Set GitHub Pages to deploy from the repository's main branch and root folder.

The homepage is `index.html`.

## Notes
- Checkout is a demo flow only.
- No real payment information is collected.
- Add the image assets referenced by the HTML files (for example `productimage.png` and `noir-automatic.png`) to the repository root when ready.
